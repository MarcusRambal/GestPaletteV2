export interface ProductProps {
  name: string;
  type: string;
  price: number;
  color: string;
}

export interface ProductFormData {
  name: string;
  type: string;  
  price: number;
  color: string; 
}
